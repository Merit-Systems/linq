/** CDP x402 resource descriptions — must stay ≤400 chars. */
export const MESSAGES_CREATE_DESCRIPTION =
  "Send on iMessage, fallback to RCS then SMS. Reuses existing chats. New recipients (cold) cost more than warm; cold openers must be text-only. Exact price from the 402 quote. See discovery guidance for pricing, media, and overrides.";
